<!-- Change form_for(@review) because of the change to the reviews URL using restaurant_id -->
<%= form_for([@restaurant, @review]) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="star-rating">

  </div>

  <div class="form-group">
    <%= f.label :comment %>
    <%= f.text_area :comment, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>


<script>
  /* Don't forget to change .star-rating to #star-rating, since it's div id="star-rating" */
  $('#star-rating').raty({
    path: 'https://s3.amazonaws.com/yelpdemo-kevinp/stars/',
    scoreName: 'review[rating]'
  });
</script>
